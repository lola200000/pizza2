<header class="main-header">
    <div class="header-container">
        <a href="{{ path('pizza_index') }}" class="logo">
            🍕 Pizza Express
            <span>La meilleure pizza en ville</span>
        </a>

        <nav class="nav">
            <a href="{{ path('pizza_index') }}">Menu</a>
            <a href="{{ path('panier_index') }}">Panier</a>

            {% if app.user %}
                <a href="{{ path('app_logout') }}" class="btn-outline">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn-outline">Connexion</a>
            {% endif %}
        </nav>
    </div>
</header>

