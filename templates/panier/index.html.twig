{% extends 'base.html.twig' %}

{% block title %}Mon Panier{% endblock %}

{% block body %}
<div class="container mt-5">
    <h2 class="text-center mb-4">Mon Panier</h2>

    {% if items is empty %}
        <div class="alert alert-info text-center">
            🛒 Votre panier est vide.
        </div>
        <div class="text-center">
            <a href="{{ path('pizza_menu') }}" class="btn btn-primary">Voir le menu</a>
        </div>
    {% else %}
        <div class="row">
            {% for item in items %}
                <div class="col-md-6 mb-3">
                    <div class="card shadow-sm">
                        <div class="row g-0 align-items-center">
                            <div class="col-4">
                                <img src="{{ asset(item.pizza.image ?: 'images/pizzas/default.jpg') }}"
                                     class="img-fluid rounded-start"
                                     alt="{{ item.pizza.name }}">
                            </div>
                            <div class="col-8">
                                <div class="card-body">
                                    <h5 class="card-title">{{ item.pizza.name }}</h5>
                                    <p class="card-text">
                                        Quantité : <strong>{{ item.quantity }}</strong><br>
                                        Prix unitaire : <strong>{{ item.pizza.prix }} €</strong><br>
                                        Sous‑total : <strong>{{ item.total | number_format(2, '.', ',') }} €</strong>
                                    </p>
                                    <div class="btn-group">
                                        <a href="{{ path('panier_add', {id: item.pizza.id}) }}" class="btn btn-sm btn-success">+</a>
                                        <a href="{{ path('panier_decrease', {id: item.pizza.id}) }}" class="btn btn-sm btn-warning">-</a>
                                        <a href="{{ path('panier_remove', {id: item.pizza.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>

        <div class="text-end my-4">
            <h4>Total : <strong>{{ total | number_format(2, '.', ',') }} €</strong></h4>
        </div>

        <div class="d-flex justify-content-between">
            <a href="{{ path('pizza_index') }}" class="btn btn-primary">Continuer mes achats</a>
            <a href="{{ path('order_checkout') }}" class="btn btn-success">Passer à la commande</a>
        </div>
    {% endif %}
</div>
{% endblock %}


